#pragma once
/*
class BuildSequence
{
public:
  using Sequence_t = std::vector< std::pair< size_t, size_t >>;
public:
  BuildSequence( std::vector< std::vector< char >>& matrixChar, std::string& givenWord_ )
  {
    for( size_t i = 0; i != givenWord_.size(); ++i )
    {
      char ch = givenWord_[i];
      std::for_each( matrixChar.begin(), matrixChar.end(), []( std::vector< char >& v ) {
    }
  }
  Sequence_t GetSequence() const
  {
    return sequence_;
  }
private:
  Sequence_t sequence_;
};*/
